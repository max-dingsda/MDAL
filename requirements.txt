# Core libraries for the MDAL proxy
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
httpx>=0.24.0
PyYAML>=6.0
langdetect>=1.0.9
lxml>=4.9.0

# ---------------------------------------------------------
# Optional database drivers for the audit log (NF5)
# (Uncomment only if audit.target is not "file")
# ---------------------------------------------------------
# psycopg2-binary>=2.9.9  # for target: postgresql
# pymysql>=1.1.0          # for target: mysql
# pyodbc>=4.0.39          # for target: mssql
